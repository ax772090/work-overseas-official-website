export const analyze = {
  methods: {
    analyze (category, action, label, value, nodeid) {
      // window._czc.push(['_trackEvent', category, action, label])
      // this.$ga.event(category, action, label)
    }
  }
}
